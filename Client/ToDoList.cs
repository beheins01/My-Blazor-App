public class TodoItem   // created a class called TodoItem
{
    public string Title { get; set; }   // This is a getter and a setter for the title
    public bool IsDone { get; set; }    // This is a getter and setter do determine if the task is done
}
